interface:
  display_name: "Review Step"
  short_description: "Review completed step changes"
  default_prompt: "Use review-step to review completed code changes against the plan, update plan progress, and prepare next steps."
