# Drop shadow on the focused window only, to make it stand out without
# relying solely on the border color.
shadow = true;
shadow-radius = 18;
shadow-opacity = 0.65;
shadow-offset-x = -10;
shadow-offset-y = -10;
shadow-exclude = [
    "! focused",
    "override_redirect = true"
];
