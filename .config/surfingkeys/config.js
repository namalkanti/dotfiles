// Loaded from ~/.config/surfingkeys/config.js via "Load settings from" in
// Surfingkeys options (Advanced section).
//
// Marker mapping to confirm this file is actually being read from disk:
// press `yz` on any page and you should see a popup.
api.mapkey('yz', 'Confirm config.js loaded from disk', function() {
    Front.showPopup('surfingkeys config.js loaded from ~/.config/surfingkeys/config.js');
});
